# 需求澄清

> **生成时间**: {{TIMESTAMP}}
> **需求名称**: {{REQUIREMENT_NAME}}
> **状态**: {{STATUS}}

---

## 澄清问题清单

{{#each unclear}}
### 问题 {{@index}}: {{question}}

| 字段 | 内容 |
|------|------|
| 标题 | {{title}} |
| 内容 | {{context}} |
| 来源 | {{ref}} |
| 状态 | {{state}} |
| 用户回答 | {{answer_org}} |
| 理解总结 | {{answer}} |

{{#if sub_questions}}
#### 追问
{{#each sub_questions}}
- {{question}} → {{answer}} [{{state}}]
{{/each}}
{{/if}}

---
{{/each}}

## 问答记录

| # | 问题 | 类型 | 选项 | 用户回答 |
|---|------|------|------|----------|
{{#each dialogue}}
| {{@index}} | {{question_title}} | {{type}} | {{options}} | {{user_answer}} |
{{/each}}

---

## 范围定义

### 原始需求

{{DEMAND_DESC}}

### 动机挖掘（Phase 2 产出）

通过三层追问确定的真实动机，用于引导后续需求分析和示例映射推导方向。

| 维度 | 用户确认的结论 | 推导方向影响 |
|------|---------------|-------------|
| 触发场景 | {{trigger_scenario}} | 决定场景优先级（紧急场景 vs 日常场景） |
| 痛点 | {{pain_point}} | 决定存量行为改造方向 |
| 期望收益 | {{expected_benefit}} | 决定验收标准的衡量维度 |

### 需求背景

{{BACKGROUND}}

### 澄清结论

{{#each conclusions}}
- **{{title}}**：{{summary}}
{{/each}}

### 涉及范围 (IN / OUT)

| 类型 | 内容 |
|------|------|
| IN | {{scope_in}} |
| OUT | {{scope_out}} |

### 功能要点

{{#each feature_points}}
- {{point}}
{{/each}}

### 约束与依赖

| 类型 | 说明 |
|------|------|
| 技术约束 | {{tech_constraint}} |
| 模块依赖 | {{module_dependency}} |
| 数据依赖 | {{data_dependency}} |

### 行为场景

以下场景通过 BDD 澄清问答推导得出，每条场景对应一条可验证的行为规则，可直接用于下游开发和测试。

{{#each scenarios}}
#### 场景 {{@index}}: {{title}}

```gherkin
Feature: {{feature}}
  Scenario: {{title}}
    Given {{given}}
    When {{when}}
    Then {{then}}
```

| 字段 | 内容 |
|------|------|
| 业务规则 | {{rule}} |
| 视角 | {{perspective}} |
| 来源 | {{ref}} |
| 边缘情况 | {{edge_cases}} |

{{/each}}

### 验收标准

{{#each acceptance_criteria}}
- [ ] {{criterion}}
{{/each}}

> **说明**：行为场景章节中的 Gherkin 场景可直接作为验收标准的可执行版本，每条 Scenario 对应一条可自动化验证的验收标准。
