---
name: whalecloud-dev-tool-requirement-clarify
description: "需求澄清技能 - 分析原始需求，基于产品知识体系事实（代码、文档、需求单上下文、历史相似工单方案），逐轮向用户确认关键点，确保需求清晰可理解。"
label: 需求澄清技能
---

# 需求澄清技能

通过分析原始需求信息，严格依赖产品知识体系事实（代码、架构文档、需求文档、需求单上下文、历史相似工单方案），逐轮向用户确认关键点，确保需求清晰可理解。**严禁臆断想象**，所有分析结论必须有实际存在的内容作为依据，需要用户确认的内容必须具体可靠。

## 共享系统脚本（BASE_SCRIPTS_DIR）

与 SynapseService 交互的脚本位于技能 **`whalecloud-dev-tool-base-scripts`**（`skills/whalecloud-dev-tool-base-scripts/`）。**BASE_SCRIPTS_DIR** 为其根目录（系统提示中「研发技能：whalecloud-dev-tool-base-scripts」的 `**技能路径**:`）。`get_doc.py` 等须用 `<BASE_SCRIPTS_DIR>/scripts/...` 调用。

---

> ⚠️ **【强制警告】技能执行输出约束**
> 
> **本技能执行过程中，大模型只能输出以下内容：**
> - **提问阶段**：直接输出问题（格式不限制，可根据需要选择）
> - **完成阶段**：直接输出澄清后的内容总结
> 
> **严格禁止输出任何其他内容**（解释、说明、提示、调试信息、日志等）
> 
> **违反此约束视为技能未完成。**

---

## Parameters

| Parameter | 必填 | 说明 / 示例 |
|-----------|------|----------------|
| `DEMAND_DESC` | 是 | 原始的需求描述内容 |
| `DEMAND_IMPACT` | 是 | 原始的需求影响内容 |
| `PROD_FEATURE` | 是 | 产品包含的功能模块列表信息 |
| `PROD` | 是 | 产品名称 |
| `SYNAPSE_URL` | 否 | SynapseService 服务地址，如 `192.168.1.100:8080`。用于获取产品架构文档 |
| `TMP_DIR` | 否 | 临时文件目录，默认 `./.tmp/`，其下存储 `docs/`（架构文档） |
| `SIMILAR_DEMAND_DOC` | 否 | 相似历史工单方案文档内容。若用户提供则直接使用；未提供则调用历史相似工单搜索能力自动检索 |

> **注意**：用户回复内容存放在系统提示词上下文中，智能体需自行从上下文中提取。

> **知识来源**：分析仅以实际存在的内容为依据，包括：代码、架构文档、需求文档、需求单上下文（DEMAND_DESC、DEMAND_IMPACT、PROD_FEATURE）、历史相似工单方案文档（若提供 `{SIMILAR_DEMAND_DOC}` 则直接使用；否则通过 `hybrid_query` 搜索历史相似工单后用 `get_doc` 获取方案文档）。任何无法从上述来源验证的结论必须标注 `[待确认]`，严禁臆断想象。

---

## 核心约束（违反本技能视为未完成）

### A. 技能执行输出限制（强制约束）

**整个技能执行过程中，大模型只能输出以下内容：**

1. **提问阶段**：直接输出问题（格式不限制，可根据需要选择）
2. **完成阶段**：直接输出澄清后的内容总结

**禁止输出任何其他内容**：
- 不得输出任何解释、说明、上下文等信息
- 不得输出 "正在分析"、"请稍等"、"以下是问题" 等提示
- 不得输出代码块标记（```markdown 等），直接输出原始内容
- 不得输出任何调试信息、日志信息

**违反此约束视为技能未完成。**

### B. 产品知识体系事实依赖（强制约束）

- 需求分析**必须严格依赖**以下三类事实来源，不得凭空臆断或想象：
  | 事实来源 | 说明 |
  |---------|------|
  | **需求单上下文** | `DEMAND_DESC`、`DEMAND_IMPACT`、`PROD_FEATURE` — 从需求单中提取的原始信息 |
  | **文档** | 架构文档（TECH_ARCH.md / FUNCTIONAL_ARCH.md）、需求文档、历史相似工单方案文档 — 从 SynapseService 或本地获取 |
  | **代码** | 项目的实际代码文件 — 通过文件系统直接读取或检索工具获取 |
- 需求分析中凡涉及**模块路径、接口、依赖关系、关键符号**等结论，必须在代码或文档中有对应支撑依据，并标注来源。
- 无法通过上述事实来源验证的条目必须标注 **`[待确认]`**，不得虚构。
- 若某文档源未获取成功，涉及该文档的分析标注 `[待补充-文档未获取]`。

### C. 问题生成

大模型根据需求分析结果自行构造问题。

**输出限制**：
- 不得输出任何解释、说明、上下文等信息

### D. BDD 驱动的需求澄清（强制约束）

本技能采用 BDD（行为驱动开发）思想指导需求澄清过程，核心原则：

#### D1. 示例驱动：用具体场景代替抽象分类

**生成问题时必须先推导具体示例，再将示例转化为选择题。**

禁止：
- 生成纯抽象分类问题，如"限流策略的类型？"（选项：令牌桶/漏桶/固定窗口）
- 生成无场景的判断题，如"是否需要审批？"（选项：是/否）

要求：
- 每个问题必须包含一个**假设场景**，描述具体的前置条件和操作
- 选项必须描述**不同行为结果**，而非单纯分类
- 场景中的角色、操作、状态要具体可感知

**示例对比**：

| 类型 | ❌ 禁止（抽象分类） | ✅ 要求（示例驱动） |
|------|-------------------|-------------------|
| 单选题 | 限流策略的类型？A.令牌桶 B.漏桶 C.固定窗口 | 假设某接口当前限流为"中"档，运维人员想调高，限流档位表示方式？A.固定三级低/中/高 B.数字1-1000自定义 C.固定五档 D.自定义标签绑定数值 |
| 判断题 | 变更是否需要审批？ | 假设开发人员小李想将自己接口的限流从"低"调为"高"，操作流程？A.直接修改无需审批 B.修改后通知运维 C.提交申请运维审批 D.仅运维可修改 |
| 判断题 | 是否立即生效？ | 假设有3个消费者正使用该限流策略处理请求（按"中"档限流），变更为"高"后的行为？A.立即生效 B.当前周期结束后下周期用新值 C.需重启服务 D.需人工确认 |

#### D2. 三视角问题：业务/开发/测试

**深度澄清阶段（非首次提问）的每条业务规则必须从三个视角各生成至少一个问题。**

| 视角 | 问题模板 | 关注点 | 存量产品中的特殊意义 |
|------|---------|--------|-------------------|
| 👔 业务视角 | "当 X 时，期望的**业务结果**是什么？" | 业务意图和期望效果 | 确认业务意图，避免做错东西 |
| 💻 开发视角 | "这个变更会影响**现有哪些模块/接口**？内部如何实现？" | 技术实现和存量影响 | 识别存量代码的影响范围 |
| 🧪 测试视角 | "如果 Y（异常情况），**应该怎样处理**？" | 边缘情况和异常路径 | 暴露存量产品中最容易出问题的场景 |

**三视角问题生成规则**：
- 首轮范围圈定问题不受三视角约束（聚焦范围即可）
- 深度澄清阶段，每条业务规则至少生成 3 个问题（业务×1 + 开发×1 + 测试×1）
- 测试视角问题**必须包含异常或边缘场景**，不能是正常路径
- 每个问题的 context 字段中标注视角标签：`[业务视角]`、`[开发视角]`、`[测试视角]`

#### D3. 业务规则拆解：示例映射四色法

**分析需求时必须使用示例映射（Example Mapping）拆解业务规则。**

| 颜色 | 含义 | 在本技能中的对应 |
|------|------|----------------|
| 🟡 黄色 | 用户故事/特性 | DEMAND_DESC 原始需求 |
| 🔵 蓝色 | 业务规则（验收标准） | 从需求中提取的行为规则 |
| 🟢 绿色 | 具体示例（场景） | 转化为问题的假设场景 |
| 🔴 红色 | 问题/不确定之处 | 必须生成的澄清问题 |

**拆解流程**：
1. **黄色卡片前置确认**：先通过需求动机挖掘（三层追问：触发场景 → 痛点 → 期望收益）确认黄色卡片（用户故事）的**真实动机**，而非仅使用 DEMAND_DESC 的表面文字
2. 基于动机确认后的黄色卡片，拆解为蓝色卡片（业务规则列表）
3. 为每条蓝色卡片推导绿色卡片（具体示例），推导方向由动机确认结果引导
4. 识别红色卡片（无法从代码、文档或需求单上下文确认的问题）
5. 绿色卡片转化为示例驱动的问题选项，红色卡片转化为判断题或开放题

---

## 工作流程

```
技能调用流程（四阶段流水线）：

本技能采用四阶段流水线结构，每个阶段有明确的输入、产出和职责边界。
阶段之间形成依赖链：Phase 1 的产出是 Phase 2 的输入，Phase 2 的产出是 Phase 3 的输入，以此类推。

【阶段概览】
Phase 0 — 环境准备：参数校验 + 目录创建 + 文档获取（仅首次执行）
Phase 1 — 代码范围圈定：文档分析 + 模块定位 + 范围确认问题
Phase 2 — 需求背景挖掘：三层动机追问 + 背景分析（触发场景 → 痛点 → 期望收益）
Phase 3 — 用户故事：示例映射四色卡片 + 规则确认（蓝卡 → 绿卡 → 红卡）
Phase 4 — 深度澄清：三视角问题澄清（最终交付）
```

---

### Phase 0 — 环境准备（首次调用必执行）

**触发条件**：首次调用技能（需判断是否已完成环境准备）

**执行步骤**：

  0a. 校验必填参数：对照 Parameters 章节，校验所有标记为"是"的参数均已提供

  0b. Python 命令适配：优先使用 `python3`，若不可用则尝试 `py`（Windows），均不可用则尝试 `python`。以下用 `{PYTHON}` 指代实际可用的 Python 命令。

  0c. 创建临时目录（若不存在）：
        → mkdir -p {TMP_DIR} {TMP_DIR}/docs

  0d. 尝试获取产品架构文档，保存到 {TMP_DIR}/docs/（若 `SYNAPSE_URL` 已提供）：
        {PYTHON} <BASE_SCRIPTS_DIR>/scripts/get_doc.py --doc_type=产品架构 --server_url {SYNAPSE_URL} --prod {PROD} --doc_name=TECH_ARCH.md --output {TMP_DIR}/docs
        {PYTHON} <BASE_SCRIPTS_DIR>/scripts/get_doc.py --doc_type=产品架构 --server_url {SYNAPSE_URL} --prod {PROD} --doc_name=FUNCTIONAL_ARCH.md --output {TMP_DIR}/docs
        若 `SYNAPSE_URL` 未提供或文档下载失败，标注 `[待补充-架构文档未获取]`，继续执行。

  0e. 获取历史相似工单方案文档：
        - 若 `SIMILAR_DEMAND_DOC` 已提供：直接将文档内容保存到 `{TMP_DIR}/docs/` 目录下使用
        - 若 `SIMILAR_DEMAND_DOC` 未提供且 `SYNAPSE_URL` 已提供：调用历史相似工单搜索能力自动检索
        - 若 `SYNAPSE_URL` 未提供、搜索无结果或文档下载失败，标注 `[待补充-历史方案文档未获取]`，继续执行。

**产出**：环境准备完成，架构文档及历史方案文档已获取（如可用）

**后续动作**：进入 Phase 1

---

### Phase 1 — 代码范围圈定

**触发条件**：环境准备已完成，需要确定代码检索范围

**输入**：架构文档（TECH_ARCH.md + FUNCTIONAL_ARCH.md，如可用）+ 历史相似工单方案文档（如可用）+ DEMAND_DESC + DEMAND_IMPACT + PROD_FEATURE

**执行步骤**：

  1a. 分析已有文档，定位与需求相关的功能模块，提取模块名称和描述
        - 优先从 `PROD_FEATURE` 中匹配相关功能模块
        - 结合架构文档（如可用）理解模块间的依赖和层次关系
        - 标注每个结论的来源（来源于 PROD_FEATURE / TECH_ARCH.md / FUNCTIONAL_ARCH.md）

  1b. **参考历史相似工单的改造模块**（若历史方案文档在 Phase 0 中已获取）：
        - 阅读历史相似工单方案文档，提取其中涉及的改造模块清单
        - 对比 1a 中分析的模块，标记历史工单中已改造过和未涉及的模块
        - 标注来源为历史方案文档的具体章节

  1c. 根据 DEMAND_DESC + DEMAND_IMPACT + PROD_FEATURE，推导初步搜索关键词

  1d. 在项目代码中搜索相关模块和入口（通过文件系统直接检索）：
        - 根据 1a 定位的模块路径，搜索对应的代码文件和目录
        - 搜索核心入口函数、接口定义等关键符号
        - 标注每项检索结果的来源路径

  1e. 汇总分析结果，形成"相关模块清单 + 核心入口文件/函数"
        - 无法从文档和代码中确认的内容标注 `[待确认]`

  1f. 生成范围圈定问题：
        - 涉及哪些功能模块？（列出分析到的相关模块，标注来源；若有历史工单参考，列出历史工单中改造过的模块供用户对比确认）
        - 核心应用入口或触发场景是什么？（用具体文档/代码位置描述）
        - 是否需要排除某些模块或场景？
        **注意**：范围圈定问题遵循约束 D1 示例驱动，但不需要三视角标注

**产出**：
- 内部维护：相关模块/入口清单（供后续阶段使用）
- 对外输出：问题（等待用户确认）

**后续动作**：输出问题 → 等待用户回答 → 智能体从上下文提取用户回答 → 进入 Phase 2

---

### Phase 2 — 需求背景挖掘

**触发条件**：用户已回答范围圈定问题（智能体从上下文提取用户回答内容）

**输入**：Phase 1 的范围清单 + 历史相似工单方案文档（如可用）+ DEMAND_DESC + DEMAND_IMPACT + PROD_FEATURE

**执行步骤**：

  2a. 从上下文提取用户回答，更新范围圈定信息

  2b. 参考历史相似工单的改造原因（若历史方案文档已获取）：
        - 阅读历史相似工单方案文档，提取其中记录的改造原因和背景
        - 识别历史工单解决的问题（痛点）和业务场景（触发场景）
        - 与当前需求进行对比，标注异同点
        - 作为 2d 动机挖掘的参考输入，帮助生成更有针对性的问题选项

  2d. **需求动机挖掘**（遵循约束 D1 示例驱动）：
        对原始需求从三个层面逐层追问，生成动机澄清问题：

        第一层：触发场景 — "这个需求是在什么业务场景下产生的？"
        示例驱动格式，例如：
          "假设您是一名后端开发人员，请问产生'API限流策略在线变更'需求的典型场景是？"
          A.大促/突发流量期间临时放量  B.日常运维效率优化  C.故障恢复时紧急放开限制  D.新业务上线前的规划调整

        第二层：痛点与期望 — "当前不支持此功能时，遇到了什么问题？"
        示例驱动格式，例如：
          "假设您发现某个接口突然被限流拒绝大量请求，当前只能通过配置中心离线调整限流策略，这个过程中最大的痛点是？"
          A.需要重启服务导致业务中断  B.调整生效周期太长影响用户体验  C.无法区分核心和非核心接口的限流配额  D.运维操作步骤繁琐容易出错

        第三层：期望收益 — "解决了这个痛点后，期望达到什么业务效果？"
        示例驱动格式，例如：
          "假设API限流策略在线变更功能已上线，您最期望看到的第一个改善是？"
          A.故障恢复时间从小时级缩短到分钟级  B.大促期间可动态调整限流无需提前规划  C.运维操作从5步简化为1步  D.不同业务线的接口不会互相抢占限流配额

        **动机挖掘的价值**：决定后续示例映射的推导方向——
        - 触发场景 → 决定场景优先级（紧急场景 vs 日常场景，边缘情况不同）
        - 痛点 → 决定存量行为改造方向（"不能在线调" vs "调了但太慢"）
        - 期望收益 → 决定验收标准的衡量维度（响应速度 vs 操作简化 vs 资源隔离）

        **历史工单参考**：若 2b 中提取了历史工单的改造原因，将其作为动机挖掘问题的辅助选项或参考提示，例如在 context 中标注"历史上类似需求的改造原因是...，当前需求是否与之一致？"

  2c. 结合用户圈定的模块范围和入口场景，推导关键搜索线索和关键词（用于后续文档/代码分析）

  2d. 生成动机挖掘问题（遵循约束 D1 示例驱动）：
        - 动机挖掘问题（3题）：触发场景 + 痛点与期望 + 期望收益
        - 若有历史工单参考，在问题 context 中注明历史类似需求的改造原因，供用户对比确认

**产出**：
- 内部维护：动机挖掘结果（触发场景 + 痛点 + 期望收益）
- 对外输出：问题（等待用户回答）

**后续动作**：输出问题 → 等待用户回答 → 智能体从上下文提取用户回答 → 进入 Phase 3

---

### Phase 3 — 用户故事（示例映射四色卡片）

**触发条件**：用户已回答动机挖掘问题（智能体从上下文提取用户回答内容）

**输入**：Phase 2 的动机挖掘结果 + Phase 1 的范围清单 + DEMAND_DESC

**执行步骤**：

  3a. 从上下文提取用户回答，更新动机挖掘结果

  3b. **示例映射拆解**（遵循约束 D3）：
        - **黄色卡片前置确认**：将动机挖掘的三个层面结果（触发场景、痛点、期望收益）作为黄色卡片的真实动机，而非仅使用 DEMAND_DESC 的表面文字
        - 拆解为蓝色卡片列表（业务规则），每条规则是一个独立的行为约束，推导方向由动机确认结果引导
        - 为每条蓝色卡片推导绿色卡片（具体示例），场景优先级由触发场景决定
        - 识别红色卡片（无法从代码/文档确认的问题），痛点决定哪些红色卡片优先级更高
        - **内部维护**四色卡片清单（**不得输出**到任何位置，仅供后续阶段推导使用）

  3c. 生成用户故事确认问题（示例驱动）：
        - 我们理解的需求包含以下业务规则（列出蓝卡），是否正确？
        - 每个规则的核心场景（列出绿卡示例），是否符合您的预期？
        **注意**：此步骤将四色卡片从"内部维护"转化为可交互确认的环节

  3d. **事实核验**（在生成确认问题前）：
        - 在代码和文档中检索关键路径，验证蓝色卡片中的规则是否与现有代码/文档行为一致
        - 对可通过代码或文档确认的规则，标注证据来源（文件路径、文档名称等）
        - 对无法确认的规则，保持为红色卡片，标注 `[待确认]`

**产出**：
- 内部维护：确认后的四色卡片清单（蓝卡 + 绿卡 + 红卡）
- 对外输出：问题（等待用户确认规则）

**后续动作**：输出问题 → 等待用户回答 → 智能体从上下文提取用户回答 → 进入 Phase 4

---

### Phase 4 — 深度澄清（三视角）

**触发条件**：用户已确认用户故事（智能体从上下文提取用户回答内容）

**输入**：Phase 3 确认后的蓝卡/绿卡/红卡列表 + 已有文档及代码分析结果

**执行步骤**：

  4a. 从上下文提取用户回答，更新规则确认信息

  4b. **增量事实检索**（如有必要）：
        - 基于 Phase 2 推导的关键词，在代码和文档中补充检索
        - 分析模块间的依赖关系
        - 结合架构文档和代码检索结果，分析问题答案

  4c. **三视角问题生成**（遵循约束 D2）：
        对每条蓝色卡片（业务规则），分别从三个视角生成问题：
        - 👔 业务视角：1 个问题，聚焦"期望的业务结果"
        - 💻 开发视角：1 个问题，聚焦"现有模块/接口的影响和实现方式"
        - 🧪 测试视角：1 个问题，聚焦"异常/边缘场景的处理"
        每条规则至少 3 个问题，确保三个视角均被覆盖

  4d. 问题内容必须遵循示例驱动（约束 D1），每个问题包含具体假设场景

  4e. 识别可通过代码或文档直接确认的问题，降级为判断题（在 context 中标注证据来源）

  4f. **输出问题**：将问题直接输出（不得添加任何解释、注释或额外内容）

**产出**：
- 对外输出：问题（等待用户回答）

**后续动作**：输出问题 → 等待用户回答 → 智能体从上下文提取用户回答 → 循环 Phase 4 直到无新问题 → 进入完成阶段

---

### 完成阶段 — 输出澄清内容

**触发条件**：没有新的待确认点（用户回答后无新问题产生，或用户明确表示需求已澄清完成）

**执行步骤**：

  5a. 从上下文提取所有已回答的问题和结论

  5b. 直接输出澄清后的内容总结

**产出**：直接输出的内容总结

---

## 使用脚本说明

### get_doc.py

脚本路径：`<BASE_SCRIPTS_DIR>/scripts/get_doc.py`（见技能 `whalecloud-dev-tool-base-scripts`）

用于从 SynapseService 获取产品架构文档：

| 命令 | 说明 |
|------|------|
| `--doc_type=产品架构` | 获取产品架构文档 |
| `--doc_type=产品需求` | 获取产品需求文档 |
| `--doc_type=产品方案` | 获取产品方案文档 |
| `--server_url XXX` | SynapseService 服务地址 |
| `--prod XXX` | 产品名称 |
| `--doc_name XXX` | 可选，指定文件名进行过滤（支持模糊匹配） |
| `--demand_no XXX` | 可选，指定工单号，用于获取特定工单的方案文档 |
| `--output XXX` | 可选，指定输出目录，文档将保存到此目录 |

示例：
```bash
# 获取产品架构文档
{PYTHON} <BASE_SCRIPTS_DIR>/scripts/get_doc.py --doc_type=产品架构 --server_url {SYNAPSE_URL} --prod {PROD} --doc_name=TECH_ARCH --output {TMP_DIR}/docs

---

## Error Handling

| 情况 | 处理 |
|------|------|
| 缺少必填参数（DEMAND_DESC/DEMAND_IMPACT/PROD_FEATURE/PROD） | **中止**，列出缺失参数 |
| SYNAPSE_URL 未提供或 get_doc.py 下载失败 | 标注 `[待补充-架构文档未获取]`，继续执行 |
| SIMILAR_DEMAND_DOC 未提供且搜索无结果或历史方案文档下载失败 | 标注 `[待补充-历史方案文档未获取]`，继续执行 |

---

## Error Handling

---

## 问答记录

| # | 问题 | 类型 | 选项 | 用户回答 |
|---|------|------|------|----------|
| {序号} | {问题标题} | {类型} | {选项列表} | {用户回答} |

---

## 范围定义

### 原始需求

{DEMAND_DESC}

### 动机挖掘（Phase 2 产出）

通过三层追问确定的真实动机。

| 维度 | 用户确认的结论 | 推导方向影响 |
|------|---------------|-------------|
| 触发场景 | {触发场景} | 决定场景优先级 |
| 痛点 | {痛点} | 决定存量行为改造方向 |
| 期望收益 | {期望收益} | 决定验收标准的衡量维度 |

### 需求背景

{需求背景分析}

### 澄清结论

- **{结论标题}**：{结论摘要}

### 涉及范围 (IN / OUT)

| 类型 | 内容 |
|------|------|
| IN | {范围内} |
| OUT | {范围外} |

### 功能要点

- {功能要点}

### 约束与依赖

| 类型 | 说明 |
|------|------|
| 技术约束 | {技术约束} |
| 模块依赖 | {模块依赖} |
| 数据依赖 | {数据依赖} |

### 行为场景

以下场景通过 BDD 澄清问答推导得出：

#### 场景 {序号}: {场景标题}

```gherkin
Feature: {feature}
  Scenario: {场景标题}
    Given {前置条件}
    When {操作}
    Then {期望结果}
```

| 字段 | 内容 |
|------|------|
| 业务规则 | {规则} |
| 视角 | {视角} |
| 来源 | {来源} |
| 边缘情况 | {边缘情况} |

```

