# {{PROJECT_NAME}} — 研发规范（Agent 指南）

> **文档版本**：{{STRUCTURES_VERSION}} · **生成时间**：{{GENERATED_DATE}} · **仓库**：`{{REPO_NAME}}`  
> **代码核验**：§2～§5 的关键结论须在 **`GNX_CACHE_DIR`** 上经 `read`/`grep` 核对；无法核对处标 **`[待代码确认]`**。  
> **用途**：本文件同时承载「代码结构描述（§2～§5）」与「研发期约束规则（各节 .x 约束子节）」，一体维护，不单独拆分。

---

## 目录

1. [速查约束](#1-速查约束)
2. [分层结构](#2-分层结构)
3. [语义结构](#3-语义结构)
4. [功能结构](#4-功能结构)
5. [风格结构](#5-风格结构)

---

## 1. 速查约束

> **本节最后生成**，内容从 §2～§5 各约束子节中提炼；填写时请先完成 §2～§5，再回填本节。

### 必须做

{{MUST_DO_RULES}}

<!-- 示例格式（须替换为本仓真实规则）：
- 改动代码前，先在 §2.2 确认所属层的可改性
- 实现新功能前，先检索 §4.2 基础能力清单，有现成实现则复用
- 生成代码的命名须符合 §5.1 的约定（按语言 + 标识符类型）
- 领域对象命名须与 §3.1 核心术语一致 -->

### 禁止做

{{MUST_NOT_RULES}}

<!-- 示例格式（须替换为本仓真实规则）：
- **禁止**重新实现 §4.2 中已列的基础能力（复制粘贴另起第二套）
- **禁止**修改 §2.2「禁改」区域（未经 RFC/ADR 审批）
- **禁止**使用 §3.1 术语表以外的名称替代标准领域术语
- **禁止**提交违反 §5.1 命名约定的代码（如不符合项目语言要求的前/后缀） -->

---

## 2. 分层结构

### 2.1 分层概览

> 以技术架构分层为线索；路径须来自 `GNX_CACHE_DIR` 核验。  
> **「职责」是本节核心**：须写清边界与主责，禁止只写一句口号式描述。

{{LAYERED_STRUCTURE_TABLE}}

<!-- 建议表头：
     | 逻辑层名 | 职责（须按四维写全，见下） | 典型路径前缀 | 承载能力 / 范围 | 依赖方向 | 与相邻层边界 / 本层不做什么 |

     「职责」四维（每层必须覆盖，可在单元格内用 ①②③④ 或换行区分）：
     ① 输入/上游边界：本层从哪层/哪类进程接收什么
     ② 本层核心职责：2～4 个可核查的动词+对象，含 1～2 个代表性类/包/可执行入口名（须来自本仓 grep）
     ③ 输出/下推：交付给下层/对外的内容及数据形态
     ④ 排除/不负责：至少一条，明确本层不做什么（用于与相邻层划界）

     冲突时：正文仅标 [与架构文档待对齐]，不写参考文档章节号，细节进 agent-doc-meta.json。 -->

### 2.2 改造边界规则

> **依据：§2.1 分层概览的路径与依赖方向**

{{RULE_SCOPE_BOUNDARIES}}

<!-- 须包含（指令格式，用「必须/禁止/谨慎」约束动词）：

     #### 可安全改动
     | 目录 / 层 | 说明 |
     |----------|------|
     | （来自 §2.1 依赖方向单向向下、无高扇入的层） | |

     #### 禁止或谨慎触碰
     | 目录 / 层 | 原因 |
     |----------|------|
     | （来自 §2.1 高扇入模块、暴露外部接口或底层基础设施）| |

     #### 须走 RFC/ADR 的变更类型
     - 跨层依赖新增（在上层直接引用下下层模块）
     - 对外接口变更（公开 API 签名修改）
     - 任何影响集群协议 / 数据格式的改动 -->

---

## 3. 语义结构

### 3.1 核心术语

> 术语与代码落点须能在 `GNX_CACHE_DIR` 中 grep 到，或标 `[待代码确认]`。

{{SEMANTIC_GLOSSARY_TABLE}}

<!-- 建议列：术语 | 含义（一句话） | 代码落点（路径/类/方法） -->

### 3.2 语义一致性规则

{{SEMANTIC_CONSISTENCY_RULES}}

<!-- 须包含（指令格式）：
     - 命名后缀约定（如 `*Service`、`*Handler`、`*Manager` 等），含义须在此定义，禁止混用
     - 同一功能**禁止**使用两种用户可见名称（进程/服务/命令行名须与 §3.1 代码落点一致）
     - API / 方法名 / 错误码须与功能架构用词保持一致
     - 与外部系统交互边界的命名规范 -->

---

## 4. 功能结构

### 4.1 功能与代码落点

> 代码落点须已在 `GNX_CACHE_DIR` 中验证；**不要**在表中填写参考架构章节号。

{{FUNCTIONAL_MAPPING_TABLE}}

<!-- 建议列：功能 / 能力项 | 用户结果（一句话） | 主要代码落点（路径级，已 grep 验证）| 关联层 | 备注 -->

### 4.2 基础能力组件清单（禁止重复引入）

> **本节是防止重复造轮子的核心索引**。  
> 须通过 grep 在 `GNX_CACHE_DIR` 中逐符号确认（C++ `class`、Go `type`、Python `class`、TS `export` 等按主语言选择），记录**类型或包名 + 关键公开 API + 所在文件路径**，不得仅填目录级路径。  
> 大模型读取本节后，须能**直接判断**新需求是否复用哪个类/函数，而非重新实现。

**总原则**：实现新需求前**必须**先核对下表；确认无对应实现方可扩展；**禁止**复制粘贴新建第二套。

{{BASE_CAPABILITY_INVENTORY}}

<!-- ===== 按能力分类逐项 grep 核查，无则标 [不适用] =====

建议表格格式：
| 能力分类 | 类型 / 包 / 模块名 | 关键公开 API（方法或函数 + 参数摘要） | 定义处（相对路径） | 复用方式 |
|---------|-------------|----------------------------------|--------------------|---------|

覆盖清单（基础设施目录以本仓 overview clusters 与 §2.1 为准；下列示例 grep 以 C++/类式 OOP 为主，其他语言按等义替换）：

- [ ] 网络通信：示例 grep `class.*Socket\|class.*TcpConn\|class.*HttpClient`
      **禁止**：新增网络连接时禁止自行封装，必须继承或组合上述类型。

- [ ] 线程与并发：示例 grep `class.*Thread\|class.*Mutex\|class.*Lock\|class.*Pool`
      **禁止**：创建新并发时禁止绕过本仓已有抽象；必须复用线程/池/调度类型及约定入口。

- [ ] 进程间通信（无则标 [不适用]）：示例 grep `class.*Shm\|shmget\|mmap`

- [ ] 配置加载：示例 grep `class.*Config\|GetValue\|LoadFile`
      **禁止**：禁止自行解析配置文件，必须使用已有配置类。

- [ ] 日志输出：示例 grep `class.*Log\|#define.*LOG\|WriteLog` 或各语言 logger 包名
      **禁止**：禁止使用裸输出（printf/cout/print）写业务日志，必须使用已有日志类/宏。

- [ ] 异常处理与错误码：示例 grep `class.*Exception\|enum.*Err\|define.*ERR_`
      **禁止**：禁止自定义新的错误码体系，必须在已有枚举中扩展。

- [ ] 序列化与编解码：示例 grep `class.*Codec\|Serialize\|Deserialize`

- [ ] 数据库 / 数据访问（无 DB 则标 [不适用]）：示例 grep `class.*DAO\|class.*Repository\|Execute\|Query`
      **禁止**：禁止直接调用数据库驱动裸 API，必须通过已有数据访问层。

- [ ] 缓存访问：示例 grep `class.*Cache\|Get\|Put\|Flush`
      **禁止**：禁止自行维护内存结构替代缓存，必须使用已有缓存控制器。

- [ ] 时间 / 定时器：示例 grep `class.*Timer\|class.*Clock\|GetTime`
- [ ] 内存池 / 分配器（无则标 [不适用]）：示例 grep `class.*MemPool\|class.*Allocator`
- [ ] 工具函数库：示例 grep `namespace.*Util\|class.*Helper\|Split\|Trim\|Format`
      **禁止**：工具类功能有现有实现的，禁止重新实现。

===== 以上为填写向导，生成时用真实 grep 结果逐行替换 ===== -->

---

## 5. 风格结构

> 通过 grep/read 从 `GNX_CACHE_DIR` 提取真实风格；须足够具体，使大模型阅读后能无歧义地生成与原风格一致的代码。

### 5.1 命名约定（按语言 + 标识符类型）

{{STYLE_CONVENTIONS}}

<!-- 填写说明：
- 按项目主语言分节；涉及多语言（如 C++ + Shell + SQL）须分节列出
- 规则须精确到数据类型级别（「驼峰」不够，须说明是 UpperCamel 还是 lowerCamel）
- 每条附一个 grep 验证示例（来自 GNX_CACHE_DIR）

#### [语言名，如 C++ / Java / Go / TypeScript / Python]

**变量命名**
| 标识符类型 | 规则 | 示例 | grep 验证模式 |
|-----------|------|------|---------------|
| 局部变量 — 整型 | | | |
| 局部变量 — 字符串 / 字符型 | | | |
| 局部变量 — 指针 / 引用 / 智能指针 | | | |
| 局部变量 — 布尔型 | | | |
| 局部变量 — 浮点型 | | | |
| 全局变量 | | | |
| 静态变量（函数内 static）| | | |
| 静态成员变量 | | | |
| 普通成员变量 | | | |
| 指针型成员变量 | | | |
| 常量（宏 / const / constexpr）| | | |
| 枚举值 | | | |
| 函数参数 | | | |

**类型命名**
| 标识符类型 | 规则 | 示例 | grep 验证模式 |
|-----------|------|------|---------------|
| 业务类名 / 结构体名 | | | |
| 接口类名 / 抽象基类名 | | | |
| 线程类名 | | | |
| 枚举类型名 | | | |
| typedef / using 别名 | | | |

**函数与方法命名**
| 标识符类型 | 规则 | 示例 | grep 验证模式 |
|-----------|------|------|---------------|
| 类成员方法（公有）| | | |
| 类成员方法（私有 / 保护）| | | |
| 回调函数 / Handler | | | |
| 线程入口函数 | | | |

**文件与目录命名**
| 标识符类型 | 规则 | 示例 |
|-----------|------|------|
| 头文件（.h / .hpp）| | |
| 实现文件（.cpp / .cc）| | |
| 可执行进程入口文件 | | |
| 目录名（模块级）| | |
| 目录名（分层级）| | |
| 路由 / URL path（若有 REST 接口）| | |
| 配置文件名 | | |

===== 以上为填写模板，生成时用真实代码事实替换 ===== -->

### 5.2 目录布局规律

（描述典型目录分层、子目录命名模式；来自 §2.1 分层的路径归纳）

### 5.3 错误处理惯用模式

（返回码 / 异常 / 错误对象的惯用方式；grep 验证示例）

### 5.4 日志与注释风格

（日志宏或函数名；注释语言与格式；文件头模板若有）

### 5.5 条件编译宏（若适用）

（关键 `#ifdef` / `#define` 约定；来自 Phase 2 在缓存对 Makefile/头文件的宏 grep 统计）

### 5.6 风格强制规则

{{RULE_CODING_STYLE}}

<!-- 须包含（指令格式，用「必须/禁止」约束动词；勿仅描述事实）：
     - 命名：**必须**使用 §5.1 中对应语言 + 标识符类型的规则，**禁止**使用不符约定的前/后缀
     - 日志：**禁止**使用各语言裸输出（printf/cout/print）写业务日志，必须使用 §4.2 日志能力
     - 错误：**禁止**自定义新错误码体系，必须在 §4.2 错误码枚举中扩展
     - 条件编译：**必须**使用 §5.5 中约定的宏名，**禁止**自创新宏 -->

---

## 参考架构

- 功能架构：`{{PATH_FUNCTIONAL_ARCH}}`
- 技术架构：`{{PATH_TECH_ARCH}}`

若实现与上述文档冲突，须在 PR 中说明并**同步更新**对应文档或标注「[文档滞后]」。
